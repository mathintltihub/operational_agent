# Operations Agent local runtime configuration

# Local Ollama endpoint
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Local model name available in `ollama list`
OLLAMA_MODEL=phi3:latest

# Optional: force deterministic fallback mode even when Ollama is available.
# Valid true values: 1, true, yes
FORCE_MOCK_MODE=false
